const Footer = props => {
    return (
        <footer>
            <div className="text-center" style={{backgroundColor: "grey", borderTop: "2px solid black"}}>
            </div>
        </footer>)
}

export default Footer